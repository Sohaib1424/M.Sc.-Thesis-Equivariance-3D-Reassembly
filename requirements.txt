# Core. Kaggle's GPU image already ships torch, numpy, scipy and matplotlib.
torch>=2.0
numpy>=1.24
scipy>=1.10
trimesh>=4.0
libigl>=2.5          # decompression; the Breaking Bad authors' recommendation
PyYAML>=6.0
tqdm>=4.65
matplotlib>=3.7

# Optional -- Google Drive checkpoint mirroring across Kaggle sessions.
# Omit these and training falls back to local-only checkpoints automatically.
google-api-python-client>=2.100
google-auth>=2.23

# Optional -- test suite.
pytest>=7.4
