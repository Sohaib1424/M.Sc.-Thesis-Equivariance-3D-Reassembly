[pytest]
testpaths = tests
# Only the project root: pytest puts tests/ on sys.path itself when there is no
# tests/__init__.py, which is what makes `from conftest import ...` work.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
